# 个人网站 · 规格文档（spec.md）

> 版本：v1.1 · 状态：已实现
> ⚠️ v1.1 变更：站点已从「暗色霓虹 / 赛博」改为「**暖琥珀 · 编辑体**」（米白暖底 + 琥珀橙 + 衬线），并把首页/作品集/关于/联系合成单页 `index.html`。§6 视觉方向以实际 `assets/css/variables.css` 与 `DESIGN.md v1.1` 为准。
> 文档负责人：主程序（lead-dev）　| 视觉审核：美术设计师（art-designer）

---

## 1. 站点定位

一个「作品集 + 简历」双合一展示网站。核心目标：让访问者在 30 秒内了解「这个人是谁、做过什么、能提供什么价值」，并留下明确的行动出口（联系作者 / 查看作品 / 获取简历）。

- 不是博客、不是电商、不是新闻站。
- 定位语（可作首页 HERO 副标题）：**「数字作品集 / 简历 · 暗色霓虹风格」**
- 内容主体：作品展示（作品集）+ 个人经历/技能/教育（简历）。
- 视觉记忆点：暗色底 + 霓虹光效 + 赛博氛围，形成独特品牌识别。

## 2. 目标用户

| 用户类型 | 需求 | 关键打动点 |
| --- | --- | --- |
| 潜在雇主 / HR | 快速判断技能匹配度、有无作品 | 作品集精炼、简历信息清晰、联系入口显眼 |
| 设计 / 开发同行 | 评估技术能力与审美水平 | 霓虹视觉质感、代码与素材使用规范、交互细节 |
| 客户 / 合作方 | 判断是否能委托项目 | 案例展示、服务/能力说明、明确联系方式 |
| 偶然访客 | 对人有好奇，想了解更多 | 关于页的个人故事、个性表达 |

**核心 KPI（浏览行为目标）**：访客到达 → 对作品或简历产生兴趣 → 触发联系。引导路径单一清晰。

## 3. 页面结构（每页章节）

### 3.1 首页（index.html）
1. **Hero 区**：名字 + 定位语（数字作品集/简历）+ 霓虹背景 + 主 CTA（查看作品集 → /portfolio）。
2. **核心理念/一句话介绍**：一句话说明「我做什么、我擅长什么」。
3. **精选作品（Featured Works）**：3–4 个代表作卡片，快速跳转作品集。
4. **技能亮点（Skills）**：技术栈/能力标签墙，分类：前端、设计、工具。
5. **关于预告（About teaser）**：一段个人简介 + 引导到 /about。
6. **联系 CTA 条（Footer 前）**：CTA「让我们聊聊」→ /contact。

### 3.2 作品集页（portfolio.html）
1. **页头**：标题「作品集」+ 简短说明 + 筛选（可选，纯前端 JS 实现，按标签：全部/网页/应用/视觉）。
2. **作品网格（Grid）**：列表式卡片（缩略图 + 标题 + 标签 + 年份）。
3. **作品详情**（点击展开或跳转至单页 /work-<slug>.html，二选一，见 4.2）：
   - 图片/视频预览、项目简介、角色职责、技术栈、访达链接（GitHub / 线上演示）。
4. **空态/加载态**（如启用筛选）：无匹配时的友好提示。

### 3.3 关于页（about.html）
1. **头像/视觉区**：个人形象（可用几何/抽象头像，见素材原则）。
2. **自我介绍**：经历、价值观、特长。
3. **简历/经历（Timeline）**：时间轴方式罗列教育、工作、项目经历。
4. **技能与工具**：能力清单，与首页呼应但更详细。
5. **下载简历**：可下载 PDF 简历的按钮（如提供）。

### 3.4 联系页（contact.html）
1. **联系标题** + 说明「欢迎合作/交流」。
2. **联系表单**：姓名、邮箱、留言（纯前端，无后端，见 6.2）。
3. **直接渠道**：邮箱、GitHub、社交链接按钮。
4. **简历/作品下载（可选）**。

## 4. 信息架构（Information Architecture）

```
个人网站（根）
├── index.html          首页
├── portfolio.html      作品集
│   └── （可选）work-<slug>.html  作品详情（多个）
├── about.html          关于 / 简历
├── contact.html        联系
└── assets/
    ├── css/
    │   ├── variables.css   设计令牌（颜色/字号/间距/霓虹色板）
    │   ├── base.css        重置与基础排版
    │   ├── components.css  通用组件（nav/按钮/卡片/标签）
    │   └── pages/          每页专属样式（可合并为单文件）
    ├── js/
    │   ├── main.js         导航/交互/筛选/表单占位等
    │   └── data.js         作品数据（供 JS 渲染，可选）
    └── img/                图片素材（遵守 7 素材原则）
```

**层级深度**：所有页面从根可达，导航全站统一，页面间平级（2 层以内）。

## 5. 导航（Navigation）

- **主导航（全站固定，页头）**：`首页` `作品集` `关于` `联系`，始终高亮当前页。
- **移动端**：汉堡菜单/抽屉，逻辑与桌面一致。
- **Footer 导航**：重复主导航 + 社交链接 + 版权。
- **面包屑**：仅在作品详情页显示（可选：`首页 / 作品集 / <作品名>`），加深站点可读性。
- **CTA**：首页 HERO 与 Footer 前提供「查看作品集 / 联系我」等行动引导。

## 6. 视觉方向描述（暗色霓虹 / 赛博）

### 6.1 色彩（Design Tokens）
- **主背景**：#0a0a0f（近黑深蓝）。
- **表面/卡片**：#12121a / #16161f。
- **主霓虹色（文字/高亮）**：青色 #00e5ff（Cyan）。
- **辅霓虹色**：洋红 #ff2ee0（Magenta）、可能辅以紫色 #8a2be2 或绿色 #39ff14（可选点缀）。
- **文字**：正文近白 #eaeaf0，次级 #9a9ab0。
- **背景微妙渐变**：深蓝→暗紫的极弱渐变，营造赛博氛围。

### 6.2 字体
- **标题/展示**：无衬线 + 等宽混搭（如 Space Grotesk / JetBrains Mono 类的系统或开源字体）。
- **正文**：系统无衬线栈（-apple-system, Segoe UI, Roboto, sans-serif）。
- 中文使用系统中文字体栈，保证无需外网也能显示（见 7 素材原则——字体属可选增强）。

### 6.3 造型与质感
- 圆角卡片 + 浅描边，边框带轻微霓虹辉光（box-shadow / 渐变边线）。
- 霓虹高亮：按钮、链接 hover，标题带发光（text-shadow 或渐变字）。
- 赛博网格/扫描线/电路纹理可作为背景装饰（方法：纯 CSS 网格渐变、径向渐变，或 CC0 纹理图片）。
- 微交互：卡片 hover 上浮 + 辉光增强、平滑滚动、页面淡入过渡（CSS transitions）。

### 6.4 动效原则
- 动效轻量、可降级，尊重 `prefers-reduced-motion`。
- 以 hover 态、过渡为主，避免影响可读性的大幅动画。

## 7. 技术约束（Technical Constraints）

1. **纯静态**：仅 HTML + CSS + JS，无构建工具、无框架、无包管理器。**双击 index.html 即可本地打开浏览**（file:// 协议可用）。
2. **无需 Node/npm**：不产生构建产物依赖，直接以源码运行。
3. **只用一个样式文件可接受**：CSS 可按变量/基础/组件拆分，但**最终必须能脱离构建直接引用**（用普通 <link> 引入，不用 import/path alias）。
4. **JS 用原生（Vanilla）**：避免依赖；交互（导航、筛选、表单占位、发光动效）用原生 DOM API。
5. **作品数据**：既可硬编码在 HTML，也可用可选 data.js 渲染；若用 JS 渲染，需保证 file:// 下 fetch 可用性（可用内联 `<script type="application/json">` 或直接 `const works = [...]`，**避免依赖 fetch 加载本地 JSON** 以免 file:// 的 CORS 限制）。
6. **响应式**：针对手机（<768px）、平板（768–1024px）、桌面（>1024px）三档断点做适配。
7. **可访问性**：语义化标签、alt 文本、足够的色彩对比、键盘可操作、`prefers-reduced-motion` 降级。
8. **性能**：静态资源体积合理，图片格式优先 WebP/AVIF（或压缩 PNG/JPG），避免大图阻塞首屏。
9. **语言**：站点文案以中文为主，可为英文预留（可选双语、i18n 不强制）。

## 8. 素材原则（Asset Principles）

> **硬性约束：禁止自创美术/照片/图标。** 本站不生成、不自绘任何原创图形素材，所有视觉素材来源仅为以下两类之一。

### 8.1 允许的素材来源
- **A. CC0 / 开源授权素材**：确认为公共领域（CC0）或宽松开源许可（MIT / Apache / 公有领域/ Unsplash, Pexels 等免版权来源）的图片、纹理、图标。
- **B. 纯几何 + CSS**：完全由 CSS 渐变、阴影、边框、线性/径向渐变构造的视觉元素（如霓虹网格、光晕、粒子、抽象几何头像），**不依赖任何图片文件**。

### 8.2 禁止项
- 不使用受版权保护的图片、字体、图标、Logo。
- 不虚构作品截图或人物照片；如用头像，须为几何抽象头像或用户可自替换的占位。
- 美术设计师的产出若属于「原创图画/插画」，**默认不纳入本网站正文素材**；如确需使用，须先取得清晰授权来源，并记录 URL（适用素材原则例外，需单独确认）。

### 8.3 来源记录（Attribution）
- 每张使用的图片/图标/纹理须在仓库 `assets/ATTRIBUTIONS.md`（或 spec 附录）记录：**素材名称、来源名称、来源 URL、许可（CC0/MIT 等）**。
- 卡片与视觉元素旁可附「图片来源」小字（如有要求）。
- 不得在来源 URL 缺失的情况下引用任何外部素材。

### 8.4 需在最终页面填写的外部素材来源（示例占位）
| 素材 | 用途 | 来源预期 |
| --- | --- | --- |
| 霓虹纹理/网格 | 背景装饰 | 处理为 CC0 图片，或纯 CSS 网格渐变（推荐后者） |
| 作品缩略图 | 作品卡片 | 开发者自带 / CC0 占位图 / 纯 CSS 几何占位 |
| 图标 | 社交/技能 | 开源图标集（如简单几何 SVG）或文字符号 |

> 推荐：**默认采用「纯几何 + CSS」方案**，可最大程度规避授权风险且与赛博风格天然契合。

## 9. 交付与验收核对

- [ ] 4 个页面齐全（首页/作品集/关于/联系），导航全站统一。
- [ ] 暗色霓虹/赛博视觉方向在公司确认的设计令牌内实现。
- [ ] 双击 index.html 可直接打开（file:// 无报错）。
- [ ] 响应式三档断点正常。
- [ ] 无任何自创/无版权来源的美术素材；素材均记录来源 URL 与许可。
- [ ] 通过 tester 的验收测试（详见后续测试用例）。

---

> 文档由 **lead-dev** 编写，视觉部分待 **art-designer** 复核后方可进入 to-tickets / implement 阶段。
